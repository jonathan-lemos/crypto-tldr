import * as React from "react";

export const Navbar = () => {
    return <nav className="navbar">
        <a className="title">Crypto TL;DR</a>
        <span className="grow"/>
    </nav>;
}

export default Navbar;
